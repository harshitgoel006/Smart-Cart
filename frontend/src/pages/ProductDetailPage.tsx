import { useEffect, useState } from 'react'
import { Link, useParams } from 'react-router-dom'
import type { Product } from '../shared/types'
import { getJson, sendJson } from '../shared/api'
import { formatPrice, getNumber, productImage } from '../shared/formatters'
import { ProductSection } from '../components/ProductSection'

export function ProductDetailPage() { const productId = useParams().id || ''; const [product, setProduct] = useState<Product | null>(null); const [related, setRelated] = useState<Product[]>([]); const [selectedImage, setSelectedImage] = useState(''); const [message, setMessage] = useState(''); const [loading, setLoading] = useState(true); useEffect(() => { Promise.all([getJson<Product>(`/products/product/${productId}`), getJson<Product[]>(`/products/product/${productId}/related?limit=4`)]).then(([data, relatedData]) => { setProduct(data); setSelectedImage(productImage(data)); setRelated(Array.isArray(relatedData) ? relatedData : []) }).catch((error: Error) => setMessage(error.message)).finally(() => setLoading(false)) }, [productId]); if (loading) return <main className="detail-page section"><div className="product-skeleton detail-skeleton" /></main>; if (!product) return <main className="section empty-page"><h1>Product unavailable</h1><p>{message}</p><Link className="primary-button" to="/products">Back to products</Link></main>; const images = product.images?.map((image) => image.url).filter(Boolean) as string[] || [productImage(product)]; const price = getNumber(product.finalPrice || product.price); const addToCart = async () => { try { await sendJson('/carts/add', 'POST', { productId: product._id, quantity: 1 }); setMessage('Added to your bag') } catch (error) { setMessage((error as Error).message) } }; return <main className="detail-page section"><div className="detail-layout"><div className="gallery"><div className="gallery-main"><img src={selectedImage || productImage(product)} alt={product.name} /></div><div className="gallery-thumbs">{images.map((image) => <button className={selectedImage === image ? 'selected' : ''} onClick={() => setSelectedImage(image)} key={image}><img src={image} alt="" /></button>)}</div></div><div className="detail-copy"><span className="eyebrow">{product.brand || 'SmartCart selection'}</span><h1>{product.name}</h1><div className="detail-rating">★ {Number(product.ratings || 0).toFixed(1)} <span>({product.reviews || 0} reviews)</span></div><div className="detail-price">{formatPrice(price)} {getNumber(product.price) > price && <del>{formatPrice(product.price)}</del>}</div><p>{product.description}</p><div className="stock-note">{(product.stock || 0) > 0 ? `In stock · ${product.stock} available` : 'Currently out of stock'}</div><button className="primary-button detail-add" onClick={addToCart} disabled={!product.stock}>Add to bag <span>↗</span></button>{message && <div className="inline-message">{message}</div>}<div className="detail-promises"><span>Free delivery over ₹999</span><span>Easy 7-day returns</span><span>Secure checkout</span></div></div></div>{related.length > 0 && <ProductSection title="You may also like" subtitle="More considered finds from this collection." products={related} loading={false} />}</main> }
