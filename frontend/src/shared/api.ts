import { API_BASE } from './constants'

function headers(): Record<string, string> { const token = sessionStorage.getItem('smartcart.accessToken'); return token ? { Authorization: `Bearer ${token}` } : {} }
export async function getJson<T>(path: string): Promise<T> { const response = await fetch(`${API_BASE}${path}`, { credentials: 'include', headers: headers() }); const body = await response.json(); if (!response.ok || body.success === false) throw new Error(body.message || 'Unable to load SmartCart data'); return (body.data ?? body) as T }
export async function sendJson<T>(path: string, method: string, payload?: unknown): Promise<T> { const response = await fetch(`${API_BASE}${path}`, { method, credentials: 'include', headers: { 'Content-Type': 'application/json', ...headers() }, body: payload ? JSON.stringify(payload) : undefined }); const body = await response.json(); if (!response.ok || body.success === false) throw new Error(body.message || 'Request could not be completed'); return (body.data ?? body) as T }
export async function sendForm<T>(path: string, formData: FormData): Promise<T> { const response = await fetch(`${API_BASE}${path}`, { method: 'POST', credentials: 'include', body: formData }); const body = await response.json(); if (!response.ok || body.success === false) throw new Error(body.message || 'Request could not be completed'); return (body.data ?? body) as T }
export async function getBlob(path: string) { const response = await fetch(`${API_BASE}${path}`, { credentials: 'include', headers: headers() }); if (!response.ok) { const body = await response.json().catch(() => ({})); throw new Error(body.message || 'Download failed') }; return response.blob() }
