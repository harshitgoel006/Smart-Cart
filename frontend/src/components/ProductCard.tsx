import { Link } from 'react-router-dom'
import type { Product } from '../shared/types'
import { formatPrice, getNumber, productImage } from '../shared/formatters'
export function ProductCard({ product }: { product: Product }) { const originalPrice = getNumber(product.price); const finalPrice = getNumber(product.finalPrice || product.price); return <article className="product-card"><Link to={`/products/${product._id}`}><div className="product-card__image-wrap"><img src={productImage(product)} alt={product.name} loading="lazy" /><span className="product-card__wish">♡</span>{(product.discountPercentage || 0) > 0 && <span className="product-card__badge">-{product.discountPercentage}%</span>}</div><div className="product-card__content"><span className="eyebrow">{product.brand || 'SmartCart select'}</span><h3>{product.name}</h3><div className="rating-row"><span>★ {Number(product.ratings || 0).toFixed(1)}</span><span className="muted">({product.reviews || 0})</span></div><div className="price-row"><strong>{formatPrice(finalPrice)}</strong>{originalPrice > finalPrice && <del>{formatPrice(originalPrice)}</del>}</div></div></Link></article> }
